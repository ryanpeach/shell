return {
    "vito-c/jq.vim",
    lazy = true, -- Lazy load the plugin
    cmd = {"Jq"}, -- Only load when the Jq command is called
    config = function()
        -- You can add additional configuration here if needed
    end
}

